# TODO

No open items.

## Done

- [x] Add SSH host-key verification (TOFU + Replit pin) — shipped in 0.1.2
  (`src/hostVerification.ts`, wired in `src/ssh.ts`; CHANGELOG entry
  `replit-ssh-001` under Security).
