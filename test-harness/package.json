{
  "name": "@mindstone/mcp-test-harness",
  "version": "0.1.0",
  "description": "Shared test harness for MCP connector packages — InMemoryTransport + stdio client, MSW helpers, bridge mock, temp-config",
  "license": "FSL-1.1-MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "msw": "^2.13.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "fast-check": "^4.8.0",
    "typescript": "^5.8.2",
    "vitest": "^4.1.3"
  },
  "peerDependencies": {
    "vitest": ">=4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
